{
  "name": "pr-v1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "server": "npm run dev",
    "build":"npm run build --prefix ../frontend",
    "client": "npm run dev --prefix ../frontend",
    "install":"npm i && npm i --prefix ../frontend",
    "conc": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "zh_rifat",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^128.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
